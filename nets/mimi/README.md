#### This directory contains the model used to train on MIMII dataset, the complete codes for the whole training process will be updated later. THe network uses the same M-SVDD module with different backbone and test data. The aim of the experiment is to validate the generalization ability of the proposed M-SVDD module.

